# QuantEcon mystmd build identifier
#
# This file records *which QuantEcon-specific squash commits are in our
# `main`*, identified by `qe-vN` git tags on chosen checkpoint commits.
# It is the diagnostic / traceability artifact — lecture builds can cat
# it to log what fork state they're using.
#
# Upstream status is tracked separately in `UPSTREAM-PRS.yml`. That file
# plans how these squash commits get bundled (or split) into upstream
# PRs, and records the upstream PR / merge status. Cross-reference is
# by commit SHA: each `merge_sha` here appears in some upstream
# candidate's `commits` list there.
#
# Per-PR landing appends the feature to `merged_features` with its squash
# `merge_sha` and `tag: null`. The `tag:` field is filled in later when a
# `qe-vN` tag is cut.
#
# Tags can be cut per-PR (one tag per feature, easy traceability) or
# batched at a checkpoint (one tag covering several merged features
# ready to deploy to book-dp1 / book-dp2 for testing) — pick whichever
# fits the cadence.
# To cut a tag, do the metadata update *first* so the tagged commit's
# tree is self-consistent:
#   1. Open a doc PR updating this file: bump `qe_version` to `qe-v<N+1>`
#      and set `tag: qe-v<N+1>` on each previously-untagged entry in
#      `merged_features` that is included in this tag.
#   2. Squash-merge the doc PR.
#   3. Tag the resulting `main` commit:
#         git tag qe-v<N+1> <sha> -m "qe-v<N+1>: <summary>"
#         git push origin qe-v<N+1>
#
# Doing it in this order means anyone who `cat`s VERSION.yml at tag
# `qe-v<N+1>` sees a self-consistent file (qe_version matches the tag).

qe_version: qe-v8
upstream_base: 1.10.1

merged_features:
  - id: 1
    name: myst-to-ipynb
    description: CommonMark ipynb export + image attachment embedding
    local_pr: 16
    merge_sha: a045d57d
    tag: qe-v1

  - id: 2
    name: book-numbering
    description: Book-style numbering — format, label, section-tagged TOC, auto-prefix
    local_pr: 22
    merge_sha: 032957c2
    tag: qe-v2

  - id: 3
    name: book-proof-scope
    description: Section-level scope for proof:* / figure / equation auto-prefix
    local_pr: 28
    merge_sha: 80bb9ecf
    tag: qe-v3

  - id: 4
    name: book-parts
    description: Book parts dividers (Roman-numbered) with continuous chapter counter
    local_pr: 26
    merge_sha: 5edc040d
    tag: qe-v4

  - id: 5
    name: book-section-heading-chain
    description: Full title→heading_1..6 chain per section in injectBookSectionDefaults
    local_pr: 33
    merge_sha: 043ddcb5
    tag: qe-v5

  - id: 6
    name: book-proof-counter-share
    description: Shared counters across proof:* kinds (LaTeX \newtheorem[<other>] parity)
    local_pr: 40
    merge_sha: a63d53f1
    tag: qe-v6

  - id: 7
    name: cli-show-qe-version
    description: Show qe-vN identifier in `myst --version` output (fork-only tooling, not for upstream)
    local_pr: 42
    merge_sha: 95494e41
    tag: qe-v7

  - id: 8
    name: fancy-lists
    description: Pandoc-style fancy ordered lists — alpha/roman/(i) markers parsed with style+delimiter through HTML/TeX/Typst/JATS exporters
    local_pr: 50
    merge_sha: 1fd33e3f
    tag: qe-v8

  - id: 9
    name: cite-code-span
    description: Citation scanner respects inline code-span boundaries — backticked @-tokens in brackets are code, not cite keys
    local_pr: 53
    merge_sha: 00740bc5
    tag: qe-v8

  - id: 10
    name: code-numbering
    description: numbering.code honours scope/book prefix (Listing 1.1) and captions follow the configured template (Listing, not Program)
    local_pr: 56
    merge_sha: 73854511
    tag: qe-v8

  - id: 11
    name: proof-unnumbered
    description: Bare proof/prf:proof defaults enumerated false (amsthm convention); theorem kinds unchanged
    local_pr: 57
    merge_sha: 75e07a9a
    tag: qe-v8

  - id: 12
    name: ci-deflake
    description: DOI resolver tests use recorded doi.org fixtures (TEST_LIVE_DOI=1 for live); Jupyter execution e2e tests get targeted vitest retry
    local_pr: 62
    merge_sha: e2bac528
    tag: null

  - id: 13
    name: ipynb-attachment-fixes
    description: ipynb attachment embedding — preserve image titles, resolve files/ output-folder URLs, async image reads
    local_pr: 64
    merge_sha: a34ab190
    tag: null

  - id: 14
    name: per-row-equation-numbering
    description: align/gather/alignat rows numbered per-row (amsmath \nonumber/\tag/\label semantics, alignment axis preserved); includes katex ^0.15.2 → ^0.16.21 bump
    local_pr: 81
    merge_sha: 756a4d5d
    tag: null
